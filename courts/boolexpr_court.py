#!/usr/bin/env python3
"""[СУД БУЛЕВЫХ ВЫРАЖЕНИЙ] — запись разобрана и посчитана заново; МИР ЗАМКНУТ.

Страница мира (`tools/boolforms.py`) есть выражение и его значение. Суд НЕ ЗНАЕТ дерева,
каким дом его строил: он разбирает ЗАПИСЬ спуском по старшинству («или» ← «и» ← «не» ←
скобка) и считает значение сам. Порча скобки, связки или ответа меняет его вердикт.

    ТАБЛИЦА СВЯЗКИ УЧИТ ОДНОМУ ШАГУ; ВЫРАЖЕНИЕ УЧИТ ПОРЯДКУ ШАГОВ. «Не истина и ложь» есть
    ложь, а «не ( истина и ложь )» — истина, и таблица об этом молчит.

Показ о скобках судится ещё одним законом: ОБЕ СТОРОНЫ ОБЯЗАНЫ РАЗОЙТИСЬ. Страница, где
скобки ничего не изменили, ничему не учит, и суд её не пропускает.
"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parents[1] / "tools"))
import boolforms as F  # noqa: E402
import closedworld  # noqa: E402

ЗАМКНУТЫЕ_МИРЫ = frozenset({"boolexpr"})


def _судить(строка):
    return F.судить(строка)


судить = closedworld.замкнуть(_судить, ЗАМКНУТЫЕ_МИРЫ)


def main():
    import collections
    from genesis import worlds

    подсадки = F.ПОДСАДКИ
    # «НЕ ПОЙМАНА» ИМЕЕТ ДВЕ ПРИЧИНЫ, И ОНИ ТРЕБУЮТ РАЗНОГО (14.09). Подсадка бывает
    # СУДИМА И ИСТИННА — тогда лгал сам список: строка похожа на ложь, а есть правда.
    # Бывает и НЕСУДИМА — тогда она выпала из рамки дома, и прибор, считающий её просто
    # «непойманной», молчит о том, что перестал её читать вовсе. Суд скобок так и потерял
    # четыре подсадки из семи, когда рамка вопроса переменилась.
    вердикты = [_судить(с) for с in подсадки]
    пойманы = sum(1 for судимо, истинно in вердикты if судимо and not истинно)
    немых = [с for с, (судимо, _и) in zip(подсадки, вердикты) if not судимо]
    for с in немых:
        print(f"  ПОДСАДКА ВНЕ РАМКИ (не судима, а не «не поймана»): {с[:110]}")
    итог, примеры = collections.Counter(), []
    пути = [п for п in worlds(kind="shows") if п.name == "genesis_boolexpr.txt"]
    if not пути:
        свой = (pathlib.Path(__file__).resolve().parents[1] / "datasets"
                / "genesis_boolexpr.txt")
        пути = [свой] if свой.exists() else []
        print("  мир не в манифесте — суд читает datasets/genesis_boolexpr.txt по имени")
    for путь in пути:
        for стр in путь.read_text(encoding="utf-8").splitlines():
            if not стр.strip() or стр.startswith("\x0c"):
                continue
            судимо, истинно = _судить(стр)
            итог["судимых" if судимо else "несудимых"] += 1
            if судимо and not истинно:
                итог["ложных"] += 1
                if len(примеры) < 5:
                    примеры.append(стр)
    for п in примеры:
        print(f"  ЛОЖЬ: {п[:150]}")
    поза = ("PASS" if итог["ложных"] == 0 and итог["несудимых"] == 0
            and пойманы == len(подсадки) else "FAIL")
    print(f"БУЛЕВЫ {поза}: {итог['ложных']} ложных из {итог['судимых']} судимых, "
          f"несудимых {итог['несудимых']}; подсадок поймано {пойманы} из {len(подсадки)}")
    return 0 if поза == "PASS" else 1


if __name__ == "__main__":
    sys.exit(main())
