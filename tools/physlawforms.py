#!/usr/bin/env python3
"""ДОМ ФИЗИЧЕСКИХ ЗАКОНОВ — размерность, сохранение, давление, волна.

Четыре предмета, названные судом программы отсутствующими, и каждый — ИНОГО РОДА:
размерность есть проверка, сохранение — запрет, давление — отношение, волна — связь трёх.

6 родов объявлены не здесь: их назвала СВОЯ ЖЕ КУЗНИЦА кортежем `ГРУППЫ`, и род всякой
страницы известен по тому, ЧЬЯ ФУНКЦИЯ её сделала.

    РОД, РЕШЁННЫЙ ПОСТРОЕНИЕМ, УЖЕ ОБЪЯВЛЕН — ОСТАЁТСЯ ЛИШЬ ЗАПИСАТЬ ЕГО ТАМ, ГДЕ ЕГО ИЩУТ.

ПЕРЕЕЗЖАЕТ НЕ ФУНКЦИЯ, А ЗАМЫКАНИЕ ЦЕЛИКОМ: сюда переехали все ряды, помощники и строители.
"""
import pathlib
import sys

КОРЕНЬ = pathlib.Path(__file__).resolve().parent.parent
sys.path.insert(0, str(КОРЕНЬ / "tools"))

import rugram
from plural import by_count  # noqa: E402

РОДЫ = ('размерности', 'сохранение', 'давление', 'волна', 'рассужд_давление', 'законы')

ЗАЧЕМ_РОДА = {
    "размерности": "размерность, объявленная формулой через основные: длина, масса, время",
    "сохранение": "что не меняется, когда меняется всё прочее",
    "давление": "сила, отнесённая к площади",
    "волна": "связь длины, частоты и скорости",
    "рассужд_давление": "то же давление, но с рассуждением и «почему»",
    "законы": "ступень определений фразами рода",
}

# РОДЫ, ЧЬЁ ЧИСЛО СТРАНИЦ ЕСТЬ ФАКТ О ПРЕДМЕТЕ, А НЕ ВЫБОР ДОМА (13.09).
#
#     РОД, ЧЬЁ ЧИСЛО СТРАНИЦ ЕСТЬ ФАКТ О ПРЕДМЕТЕ, НЕ ПОДЛЕЖИТ МЕРЕ РОВНОСТИ — НО ОБЯЗАН
#     БЫТЬ ОБЪЯВЛЕН ТАКОВЫМ, ИНАЧЕ ПОД ЭТИМ ИМЕНЕМ СПРЯЧЕТСЯ ВСЯКИЙ НЕДОПИСАННЫЙ.
#
# Объявление это ЧИТАЕТ `scripts/house_skew.py` и печатает поимённо: освобождение не молчит.
РОДЫ_ПО_ПРЕДМЕТУ = {
    "законы": "ступень законов: одно определение на фразу рода",
}

# РАЗМЕРНОСТЬ ОБЪЯВЛЕНА ФОРМУЛОЙ ЧЕРЕЗ ОСНОВНЫЕ: длина, масса, время.
# Её нельзя вывести из имени величины — её знают.
# ПРЕДЛОЖНЫЙ ПАДЕЖ НАЗВАН, А НЕ ВЫВЕДЕН: «измеряется в метрах», не «в
# метр». Русскую форму нельзя получить отсечением, её называют.
РАЗМЕРНОСТИ = (
    ("скорость", "speed", "L / T", "метрах в секунду",
     "metre per second"),
    ("ускорение", "acceleration", "L / T^2",
     "метрах на секунду в квадрате", "metre per second squared"),
    ("сила", "force", "M L / T^2", "ньютонах", "newton"),
    ("работа", "work", "M L^2 / T^2", "джоулях", "joule"),
    ("мощность", "power", "M L^2 / T^3", "ваттах", "watt"),
    ("давление", "pressure", "M / (L T^2)", "паскалях", "pascal"),
    ("импульс", "momentum", "M L / T",
     "килограмм-метрах в секунду", "kilogram metre per second"),
)


# ИСКОМОЕ ОБЪЯВЛЯЕТ СВОЙ ВОПРОС ОДИН РАЗ, и вопрос берёт ТЕ ЖЕ
# величины, из которых собран ответ. Замер вопросной поверхности назвал
# этот мир немым: 990 строк, вопросов ноль.
СПРОСИТЬ = {
    "law": "what does {закон} give for {x} and {y}?",
    "закон": "что даёт {закон} при {x} и {y}?",
    "whole": "does {закон} give a whole number for {x} and {y}?",
    "целое": "даёт ли {закон} целое число при {x} и {y}?",
}

# ФОРМУЛЫ РОДОВ — ЗАКОН ОТВЕТА ОТ ВЕЛИЧИН ВОПРОСА, объявлен при каждом вопросе
# (таблица родов declarations/GENERA.json — эталон суда охвата, holon 03.09).
ФОРМУЛЫ = {
    "law": "давление = сила ÷ площадь",
    "закон": "давление = сила ÷ площадь",
    "whole": "целость: сила делится на площадь?",
    "целое": "целость: сила делится на площадь?",
}
assert set(ФОРМУЛЫ) == set(СПРОСИТЬ), "формула у каждого вопроса"


def спросить(искомое, ответ, **части):
    """Вопрос и ответ одной строкой; величины у них одни и те же."""
    return f"{СПРОСИТЬ[искомое].format(**части)} {ответ}"


def размерности(шаг):
    вон = []
    for ру, en, форм, ру_пред, en_ед in РАЗМЕРНОСТИ:
        вон.append(f"размерность величины {ру} есть {форм}; "
                   f"измеряется в {ру_пред}.")
        вон.append(f"the dimension of {en} is {форм}; "
                   f"it is measured in {en_ед}.")
    return вон


def сохранение(шаг):
    """Что не рождается и не исчезает — довод от неизменного."""
    вон = []
    for i in range(16):
        a, b = 3 + (i + шаг) % 7, 5 + (i * 2 + шаг) % 9
        итог = a + b
        вон.append(f"импульс сохраняется: было {a} и {b}, стало "
                   f"{итог}, сумма не изменилась.")
        вон.append(f"momentum is conserved: {a} and {b} before, "
                   f"{итог} after, the sum did not change.")
        e1, e2 = 4 + (i + шаг) % 6, 6 + (i + шаг) % 8
        целое, часть = max(e1, e2), min(e1, e2)
        остаток = целое - часть
        вон.append(f"энергия сохраняется: {целое} "
                   f"{rugram.форма('джоуль', целое)} разделились на {часть} и "
                   f"{остаток} {rugram.форма('джоуль', остаток)}.")
        вон.append(f"energy is conserved: {max(e1, e2)} {by_count(max(e1, e2), 'joules')} split "
                   f"into {min(e1, e2)} and "
                   f"{max(e1, e2) - min(e1, e2)} {by_count(max(e1, e2) - min(e1, e2), 'joules')}.")
    return вон


def давление(шаг):
    """Сила на площадь — первая величина как ОТНОШЕНИЕ двух других."""
    вон = []
    for i in range(20):
        # ГРАНИЦА ДАВЛЕНИЯ ЕСТЬ ПОКАЗ (08.09). Площадь шла от двух, давление от трёх, и
        # «1 pascal» не стоял в своде НИ РАЗУ при 289 «pascals»; так же «1 square metre».
        # Единица площади и единица давления суть честные величины, а не крайние случаи.
        площадь = 1 + (i + шаг) % 7
        p = 1 + (i + шаг) % 10
        сила = p * площадь
        утв_ru = (f"давление = сила ÷ площадь; {сила} "
                  f"{rugram.форма('ньютон', сила)} ÷ {площадь} "
                  f"{rugram.форма('квадратный метр', площадь)} = {p} "
                  f"{rugram.форма('паскаль', p)}.")
        # АНГЛИЙСКАЯ СТОРОНА ИДЁТ ЗАКОНОМ, КАК РУССКАЯ: русская здесь звала `rugram.форма`,
        # английская держала литерал множественного — та же половинчатость, что у домов
        # физики и кибернетики.
        утв_en = (f"pressure = force ÷ area; {сила} {by_count(сила, 'newtons')} ÷ "
                  f"{площадь} {by_count(площадь, 'square metres')} = "
                  f"{p} {by_count(p, 'pascals')}.")
        вон.append(утв_ru)
        вон.append(утв_en)
        вон.append(спросить("закон", утв_ru,
                            закон="давление = сила ÷ площадь",
                            x=сила, y=площадь))
        вон.append(спросить("law", утв_en,
                            закон="pressure = force ÷ area",
                            x=сила, y=площадь))
        # WHOLENESS IS A YES/NO QUESTION (holon 03.09, value-not-verdict: a
        # question for a VALUE answered by a refusal looked like a verdict
        # frame with one polarity). The question now asks about wholeness
        # itself, and both answers lie side by side — «yes» with the
        # quotient, «no» with the reason; the court recomputes the remainder.
        # СКАЗУЕМОЕ ИДЁТ ЗА ЧИСЛОМ: «21 ньютон не даёт», но «25 ньютонов не дают».
        целая = сила + площадь if i % 2 == 0 else сила + 1
        частное = целая // площадь
        один = (rugram.форма("ньютон", целая) == rugram.форма("ньютон", 1))
        if целая % площадь == 0:
            отв_ru = (f"да: {целая} {rugram.форма('ньютон', целая)} на {площадь} "
                      f"{rugram.форма('квадратный метр', площадь)} "
                      f"{'даёт' if один else 'дают'} {целая} ÷ {площадь} = "
                      f"{частное} {rugram.форма('паскаль', частное)}.")
            отв_en = (f"yes: {целая} {by_count(целая, 'newtons')} over {площадь} square metres give "
                      f"{целая} ÷ {площадь} = {частное} "
                      f"{by_count(частное, 'pascals')}.")
        else:
            отв_ru = (f"нет: {целая} {rugram.форма('ньютон', целая)} на {площадь} "
                      f"{rugram.форма('квадратный метр', площадь)} "
                      f"{'не даёт' if один else 'не дают'} целого давления, "
                      f"{целая} не делится на {площадь} нацело.")
            отв_en = (f"no: {целая} {by_count(целая, 'newtons')} over {площадь} square metres do not "
                      f"give a whole pressure, {целая} is not divisible by {площадь}.")
        вон.append(спросить("целое", отв_ru, закон="давление = сила ÷ площадь", x=целая, y=площадь))
        вон.append(спросить("whole", отв_en, закон="pressure = force ÷ area", x=целая, y=площадь))
    return вон


def волна(шаг):
    """Период и частота обратны: их произведение есть единица."""
    вон = []
    for i in range(20):
        период = 1 + (i + шаг) % 8
        частота = 60 // период if 60 % период == 0 else 12
        период = 60 // частота
        # ПРЕДЛОГ ТРЕБУЕТ СВОЕГО ПАДЕЖА, и «в 1 секунда» неверно, как
        # неверно «a period of 1 seconds». Оборот выбран такой, где
        # падеж есть именительный счётный, — форма тогда одна и верна.
        вон.append(f"период — {период} {rugram.форма('секунда', период)}; "
                   f"частота — {частота} "
                   f"{rugram.форма('колебание', частота)} в минуту; "
                   f"{период} × {частота} = 60.")
        вон.append(f"the period is {период} "
                   f"{'second' if период == 1 else 'seconds'}; the "
                   f"frequency is {частота} per minute; "
                   f"{период} × {частота} = 60.")
        скорость = 3 + (i + шаг) % 5
        длина = скорость * период
        вон.append(f"скорость волны = длина ÷ период; {длина} "
                   f"{rugram.форма('метр', длина)} ÷ {период} "
                   f"{rugram.форма('секунда', период)} = {скорость} "
                   f"{rugram.форма('метр', скорость)} в секунду.")
        вон.append(f"wave speed = length ÷ period; {длина} {by_count(длина, 'metres')} ÷ "
                   f"{период} {'second' if период == 1 else 'seconds'} "
                   f"= {скорость} {by_count(скорость, 'metres')} per second.")
    return вон


import discourse  # noqa: E402
import laws  # noqa: E402


def рассужд_давление(шаг):
    """Давление рассуждением (дом речи): счёт-свидетель, связка, вывод
    той же формы, что утверждение мира, закон рода; «почему» — вторым зачином."""
    вон = []
    for i in range(10):
        площадь = 2 + (i + шаг) % 6
        p = 3 + (i * 3 + шаг) % 8
        сила = p * площадь
        св_en = (f"{сила} {by_count(сила, 'newtons')} ÷ "
                 f"{площадь} {by_count(площадь, 'square metres')} = "
                 f"{p} {by_count(p, 'pascals')}")
        св_ru = f"{сила} {rugram.форма('ньютон', сила)} ÷ {площадь} {rugram.форма('квадратный метр', площадь)} = {p} {rugram.форма('паскаль', p)}"
        выв_en = f"the pressure is {p} {by_count(p, 'pascals')}"
        выв_ru = f"давление — {p} {rugram.форма('паскаль', p)}"
        зак_en, зак_ru = laws.ЗАКОНЫ["physlaws"][0][2], laws.ЗАКОНЫ["physlaws"][0][3]
        if i % 2 == 0:
            вон.append(discourse.рассуждение_величины("en", f"what is the pressure of {сила} {by_count(сила, 'newtons')} on {площадь} square metres", св_en, выв_en, зак_en))
            вон.append(discourse.рассуждение_величины("ru", f"каково давление силы {сила} {rugram.форма('ньютон', сила)} на {площадь} {rugram.форма('квадратный метр', площадь)}", св_ru, выв_ru, зак_ru))
        else:
            вон.append(discourse.почему("en", f"why is the pressure of {сила} {by_count(сила, 'newtons')} on {площадь} square metres equal to {p} {by_count(p, 'pascals')}", св_en, выв_en, зак_en))
            вон.append(discourse.почему("ru", f"почему давление силы {сила} {rugram.форма('ньютон', сила)} на {площадь} {rugram.форма('квадратный метр', площадь)} равно {p} {rugram.форма('паскаль', p)}", св_ru, выв_ru, зак_ru))
    return вон


def законы(шаг):
    return laws.ступень("physlaws")

ГРУППЫ = (размерности, сохранение, давление, волна, рассужд_давление, законы)


def группы(шаг):
    """[[страница]] — ровно те группы и в том порядке, какими кузница кормит `emit_grouped`."""
    return [сделать(шаг) for сделать in ГРУППЫ]


def перебор(шаг):
    """[(страница, род)] — те же группы, но каждая под своим именем."""
    вон = []
    for род, группа in zip(РОДЫ, группы(шаг)):
        for с in группа:
            вон.append((с, род))
    return вон


def _показы():
    from layer import PASSES                              # noqa: PLC0415
    вон = {}
    for шаг in range(len(PASSES)):
        for с, род in перебор(шаг):
            вон.setdefault(с, род)
    return вон


ПОКАЗЫ = _показы()


def _самопроверка():
    """Объявление сверяется с ДЕЛОМ прохода, а не с кортежем."""
    имена = tuple(с.__name__ for с in ГРУППЫ)
    assert имена == РОДЫ, f"кортеж {имена} разошёлся с объявлением {РОДЫ}"
    сколько = len(группы(0))
    assert сколько == len(РОДЫ), f"проход даёт {сколько} групп при {len(РОДЫ)} родах"
    assert set(ЗАЧЕМ_РОДА) == set(РОДЫ), "глосса рода разошлась с объявлением"
    пустые = set(РОДЫ) - set(ПОКАЗЫ.values())
    assert not пустые, f"род объявлен и не кован: {sorted(пустые)}"


_самопроверка()
