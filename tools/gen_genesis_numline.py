#!/usr/bin/env python3
"""GENESIS layer: ЧИСЛОВАЯ ПРЯМАЯ — see tools/numlineforms.py for the law.

ОБЕ СТОРОНЫ ЛОВУШКИ ИДУТ ОДНОЙ ГРУППОЙ, и здесь это не приём, а сама мера. Сравнение цифр
верно на положительной половине прямой и ложно на отрицательной.

    ПРАВИЛО, ВЕРНОЕ НА ПОЛОВИНЕ СЛУЧАЕВ, НЕ ЕСТЬ ПОЛОВИНА ПРАВИЛА — ОНО ЕСТЬ ДРУГОЕ
    ПРАВИЛО, И ОШИБАЕТСЯ ОНО НЕ ИНОГДА, А РОВНО В ПОЛОВИНЕ ВСЕХ.

ДВИЖЕНИЕ И РАССТОЯНИЕ — одной группой: шаг вправо, расстояние без знака и равноудалённость
противоположных суть три взгляда на одно устройство прямой. ВОПРОС отдельно.
"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent))
import numlineforms as F  # noqa: E402
from layer import emit_grouped, PASSES  # noqa: E402

ЦЕЛЬ = "datasets/genesis_numline.txt"
ГРУППЫ = (("левее значит меньше", "между"),
          ("цифры лгут при минусе", "цифры не лгут без него"),
          ("шаг вправо прибавляет", "расстояние без знака", "противоположные"),
          ("спрошенное",))


def pass_groups(шаг):
    вон = []
    for язык in F.ЯЗЫКИ:
        for роды in ГРУППЫ:
            свои = [с for с, (л, р) in F.ПОКАЗЫ.items() if л == язык and р in роды]
            вон.append(свои[шаг::len(PASSES)])
    return вон


def main():
    emit_grouped(ЦЕЛЬ, pass_groups)


if __name__ == "__main__":
    main()
