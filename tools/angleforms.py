#!/usr/bin/env python3
"""ДОМ СУММЫ УГЛОВ — сто восемьдесят у ВСЯКОГО треугольника, каков бы он ни был.

    СУММА УГЛОВ ЕСТЬ СВОЙСТВО ЧИСЛА СТОРОН, А НЕ РАЗМЕРА И НЕ ФОРМЫ. Треугольник в ладонь и
    треугольник в поле дают одно и то же число; четырёхугольник даёт другое — и другое не
    оттого, что он больше, а оттого, что сторон у него четыре.

ПОВОД, ЗАМЕРЕННЫЙ 12.09 ПО ВСЕМУ СВОДУ В 39 МБ: «сумма углов» — НОЛЬ вхождений. Свод знает
слово «треугольник» (452 строки) и «прямой угол» (9), считает площади и периметры — и ни разу
не говорит того первого, что знает о треугольнике всякий: его углы в сумме дают 180°.

ВОСЕМЬ РОДОВ, И ДВА ИЗ НИХ СУТЬ ОДНА ЛОВУШКА, ПОКАЗАННАЯ ОБЕИМИ СТОРОНАМИ:

    СУММА ТРЕУГОЛЬНИКА    «60 + 60 + 60 = 180».
    ИНОЙ ТРЕУГОЛЬНИК      «90 + 60 + 30 = 180»: фигура иная, сумма ТА ЖЕ.
    ЧЕТЫРЁХУГОЛЬНИК       «100 + 80 + 110 + 70 = 360»: сумма ИНАЯ — и не от размера.
    ТРЕТИЙ ПО ДВУМ        «180 − 50 − 70 = 60»: два угла держат третий.
    МНОГОУГОЛЬНИК         «(n − 2) × 180», и это пересчитано для каждого n, а не объявлено.
    ДВА ОСТРЫХ ПРИ ПРЯМОМ «90 + 35 + 55 = 180, и 35 + 55 = 90».
    ДВУХ ПРЯМЫХ НЕ БЫВАЕТ «90 + 90 = 180, и третьему углу не осталось ничего».
    СПРОШЕННОЕ            вопрос, чей ответ есть само постоянство суммы.

ПОЧЕМУ ОБЕ СТОРОНЫ ОБЯЗАТЕЛЬНЫ. Учащийся, встретивший лишь «у треугольника всегда 180»,
перенесёт постоянство на всякую фигуру и скажет «180» о четырёхугольнике. Встретивший лишь
«у четырёхугольника 360», решит, что сумма растёт с размером.

    ПОСТОЯНСТВО, ПОКАЗАННОЕ БЕЗ СВОЕЙ ГРАНИЦЫ, ЧИТАЕТСЯ КАК ВСЕОБЩЕЕ: ЧИСЛО 180 ПРИНАДЛЕЖИТ
    НЕ ФИГУРЕ ВООБЩЕ, А ФИГУРЕ О ТРЁХ СТОРОНАХ, И СКАЗАТЬ ЭТО НАДО ВСЛУХ.

ГРАНИЦА НАЗВАНА: дом говорит о плоском треугольнике и о выпуклом многоугольнике. Углы целые,
и это не удобство, а закон свода: корпус, сказавший «60,5°», научил бы дроби там, где её
можно было не трогать.

СОРОК ДЕВЯТАЯ ЛОВУШКА РЯДА — сумма углов, принятая за свойство размера. Номер выведен по
записи свода (час ковки), а не назначен, и стои́т ЗДЕСЬ, а не только в объявлении мира:

    ДОМ, НЕ ЗНАЮЩИЙ СВОЕГО МЕСТА В РЯДУ, УЗНАЁТ О НЁМ ИЗ ЧУЖОГО ОБЪЯВЛЕНИЯ;
    А ЧИТАЮЩИЙ ДОМ МАНИФЕСТА НЕ ЧИТАЕТ.

    python3 tools/angleforms.py     # самопроверка с мутантами
"""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent))
import arithread  # noqa: E402 — чтение записи объявлено сводом, а не домом

ЯЗЫКИ = ("ru", "en")
РОДЫ = ("сумма треугольника", "иной треугольник", "четырёхугольник", "третий по двум",
        "многоугольник", "два острых при прямом", "двух прямых не бывает", "спрошенное")

# ТРОЙКИ УГЛОВ: все целые, все положительные, в сумме 180.
# ПРИБАВЛЕНО 13.09 РАДИ ЗАКОНА МАССЫ, И ПРИБАВЛЕНО ЧИСЛАМИ, А НЕ СЛОВАМИ: новых слов
# страницы не несут, и прибор «слово однажды» от них не дрогнет.
#
#     ПРИБАВКА ЧИСЛАМИ ДЕШЕВЛЕ ПРИБАВКИ СЛОВАМИ: число, какого не было, не становится
#     словом, показанным однажды.
ТРЕУГОЛЬНИКИ = ((60, 60, 60), (90, 60, 30), (50, 70, 60), (100, 40, 40),
                (45, 45, 90), (20, 80, 80), (120, 30, 30),
                (35, 65, 80), (110, 25, 45), (75, 55, 50),
                (15, 75, 90), (140, 20, 20), (55, 65, 60))
# ЧЕТЫРЁХУГОЛЬНИКИ: в сумме 360.
ЧЕТЫРЁХУГОЛЬНИКИ = ((100, 80, 110, 70), (90, 90, 90, 90), (120, 60, 120, 60),
                    (70, 110, 100, 80), (85, 95, 85, 95), (140, 40, 140, 40))
# ПРЯМОУГОЛЬНЫЕ: (острый, острый) при прямом
ПРЯМОУГОЛЬНЫЕ = ((35, 55), (20, 70), (45, 45), (10, 80), (25, 65), (40, 50))
# ЗАКОН МАССЫ ВЗЯТ У ПЕРЕПИСИ ФОРМ, А НЕ ОБЪЯВЛЕН ЗАНОВО: девять — то же число, каким мерят
# `scripts/mass_census.py` и `scripts/house_promise.py`.
ЗАКОН_МАССЫ = 9
ПЕРВЫХ = -(-ЗАКОН_МАССЫ // 2)          # сколько треугольников отдать роду суммы: на два языка

# ЧИСЛА СТОРОН ДЛЯ ОБЩЕГО ЗАКОНА
СТОРОНЫ = (3, 4, 5, 6, 8)


def _сумма(n):
    return (n - 2) * 180


РАМКИ = {
    "ru": {
        "сумма треугольника": "углы треугольника {a}°, {b}° и {c}° дают в сумме 180°: "
                              "{a} + {b} + {c} = 180. СУММА УГЛОВ ТРЕУГОЛЬНИКА ЕСТЬ 180°, "
                              "КАКОВ БЫ ОН НИ БЫЛ.",
        "иной треугольник": "у треугольника с углами {a}°, {b}° и {c}° сумма всё та же: "
                            "{a} + {b} + {c} = 180. ФИГУРА ИНАЯ, А ЧИСЛО ТО ЖЕ — СУММА НЕ "
                            "ЗАВИСИТ НИ ОТ РАЗМЕРА, НИ ОТ ФОРМЫ.",
        "четырёхугольник": "углы четырёхугольника {a}°, {b}°, {c}° и {d}° дают 360°: "
                           "{a} + {b} + {c} + {d} = 360, А НЕ 180. СУММА ЗАВИСИТ ОТ ЧИСЛА "
                           "СТОРОН, А НЕ ОТ РАЗМЕРА ФИГУРЫ.",
        "третий по двум": "два угла треугольника — {a}° и {b}°; третий найден вычитанием: "
                          "180 − {a} − {b} = {c}. ДВА УГЛА ДЕРЖАТ ТРЕТИЙ, И ИЗМЕРЯТЬ ЕГО "
                          "НЕЧЕГО.",
        "многоугольник": "у выпуклого {n}-угольника сумма углов {s}°: ({n} − 2) × 180 = {s}. "
                         "КАЖДАЯ НОВАЯ СТОРОНА ПРИБАВЛЯЕТ РОВНО 180°.",
        "два острых при прямом": "в прямоугольном треугольнике один угол 90°, а два острых "
                                 "{a}° и {b}°: 90 + {a} + {b} = 180, значит {a} + {b} = 90. "
                                 "ДВА ОСТРЫХ УГЛА ВМЕСТЕ ДАЮТ ПРЯМОЙ.",
        "двух прямых не бывает": "двух углов по {у}° в треугольнике не бывает: {у} + {у} = "
                                 "{сумма}, и третьему углу осталось бы {остаток}°, а угол в "
                                 "{остаток}° не есть угол. ГРАНИЦА ЗДЕСЬ НЕ ЗАПРЕТ, А "
                                 "СЛЕДСТВИЕ СЧЁТА.",
        "спрошенное": "у одного треугольника углы {a}°, {b}°, {c}°, у другого — {a2}°, {b2}°, "
                      "{c2}°. что у них общего? сумма: {a} + {b} + {c} = 180 и {a2} + {b2} + "
                      "{c2} = 180.",
    },
    "en": {
        "сумма треугольника": "the angles of a triangle, {a}°, {b}° and {c}°, add up to 180°: "
                              "{a} + {b} + {c} = 180. THE ANGLES OF A TRIANGLE SUM TO 180°, "
                              "WHATEVER THE TRIANGLE.",
        "иной треугольник": "for a triangle with angles {a}°, {b}° and {c}° the sum is the "
                            "same: {a} + {b} + {c} = 180. ANOTHER FIGURE, THE SAME NUMBER — "
                            "THE SUM DEPENDS NEITHER ON SIZE NOR ON SHAPE.",
        "четырёхугольник": "the angles of a quadrilateral, {a}°, {b}°, {c}° and {d}°, give "
                           "360°: {a} + {b} + {c} + {d} = 360, NOT 180. THE SUM DEPENDS ON "
                           "THE NUMBER OF SIDES, NOT ON THE SIZE OF THE FIGURE.",
        "третий по двум": "two angles of a triangle are {a}° and {b}°; the third is found by "
                          "subtraction: 180 − {a} − {b} = {c}. TWO ANGLES HOLD THE THIRD, AND "
                          "THERE IS NOTHING TO MEASURE.",
        "многоугольник": "in a convex {n}-gon the angles sum to {s}°: ({n} − 2) × 180 = {s}. "
                         "EVERY NEW SIDE ADDS EXACTLY 180°.",
        "два острых при прямом": "in a right triangle one angle is 90° and the two acute ones "
                                 "are {a}° and {b}°: 90 + {a} + {b} = 180, so {a} + {b} = 90. "
                                 "TWO ACUTE ANGLES TOGETHER MAKE A RIGHT ONE.",
        "двух прямых не бывает": "a triangle with two angles of {у}° does not exist: {у} + "
                                 "{у} = {сумма}, and {остаток}° would be left for the third, "
                                 "while an angle of {остаток}° is no angle. THE LIMIT HERE IS "
                                 "NOT A PROHIBITION BUT A CONSEQUENCE OF COUNTING.",
        "спрошенное": "one triangle has angles {a}°, {b}°, {c}°, another has {a2}°, {b2}°, "
                      "{c2}°. what do they share? the sum: {a} + {b} + {c} = 180 and {a2} + "
                      "{b2} + {c2} = 180.",
    },
}


def страницы():
    вон = {}
    for язык in ЯЗЫКИ:
        рамки = РАМКИ[язык]
        for i, (a, b, c) in enumerate(ТРЕУГОЛЬНИКИ):
            # РОД «СУММА ТРЕУГОЛЬНИКА» ИДЁТ ПО ДВУМ ПЕРВЫМ, А НЕ ПО ОДНОМУ (13.09,
            # куплено прибором «слово однажды»): страница, написанная единожды, оставляла
            # свои слова во всём своде РОВНО РАЗ.
            #
            #     СЛОВО, ПОКАЗАННОЕ ОДИН РАЗ, ЕСТЬ СЛОВО НЕПОКАЗАННОЕ.
            # ЧИСЛО ПЕРВЫХ ВЗЯТО ЗАКОНОМ МАССЫ, А НЕ РУКОЙ: рода довольно, когда на два
            # языка выходит девять страниц. Прежде здесь стояло «два», и род держался на
            # четырёх страницах при законе в девять.
            #
            #     ЧИСЛО ПОКАЗОВ, ПИСАННОЕ РУКОЙ, СТАРЕЕТ ОТДЕЛЬНО ОТ ЗАКОНА, КОТОРЫЙ ЕГО
            #     МЕРИТ, — и стареет молча.
            род = "сумма треугольника" if i < ПЕРВЫХ else "иной треугольник"
            вон[рамки[род].format(a=a, b=b, c=c)] = (язык, род)
            вон[рамки["третий по двум"].format(a=a, b=b, c=c)] = (язык, "третий по двум")
            a2, b2, c2 = ТРЕУГОЛЬНИКИ[(i + 1) % len(ТРЕУГОЛЬНИКИ)]
            вон[рамки["спрошенное"].format(a=a, b=b, c=c, a2=a2, b2=b2, c2=c2)] = (
                язык, "спрошенное")
        for a, b, c, d in ЧЕТЫРЁХУГОЛЬНИКИ:
            вон[рамки["четырёхугольник"].format(a=a, b=b, c=c, d=d)] = (
                язык, "четырёхугольник")
        for n in СТОРОНЫ:
            вон[рамки["многоугольник"].format(n=n, s=_сумма(n))] = (язык, "многоугольник")
        for a, b in ПРЯМОУГОЛЬНЫЕ:
            вон[рамки["два острых при прямом"].format(a=a, b=b)] = (
                язык, "два острых при прямом")
        # ТРИ УГЛА ВМЕСТО ОДНОГО, И ПО ТОЙ ЖЕ ПРИЧИНЕ: 90 + 90 = 180 (третьему ноль),
        # 100 + 100 = 200 и 120 + 120 = 240 (третьему меньше нуля). Довод один, случаев три.
        for у in (90, 100, 120, 95, 110):
            вон[рамки["двух прямых не бывает"].format(
                у=у, сумма=2 * у, остаток=180 - 2 * у)] = (язык, "двух прямых не бывает")
    return вон


ПОКАЗЫ = страницы()
_ПО_ДЛИНЕ = {}
for _п in ПОКАЗЫ:
    _слова = _п.split(" ")
    _ПО_ДЛИНЕ.setdefault(len(_слова), []).append(_слова)


def судить(строка):
    """(судимо, истинно): страница мира истинна, если её порождает объявленная рамка И
    ЕСЛИ ВСЯКАЯ ЗАПИСЬ В НЕЙ ПЕРЕСЧИТЫВАЕТСЯ РАЗБОРОМ (`arithread`).

    СТРАНИЦА, КОТОРУЮ РАМКА НЕ ПОРОЖДАЕТ, НЕ ЕСТЬ ЛОЖЬ ЭТОГО СУДА — ей нечего сказать;
    ложь о СВОЁМ мире говорит замыкание, а не начало строки.
    """
    с = строка.strip()
    if not с:
        return False, False
    if с not in ПОКАЗЫ:
        return (True, False) if _порча_одним_словом(с) else (False, False)
    return True, arithread.записи_верны(с)


def _порча_одним_словом(с):
    """Строка, разошедшаяся с показом РОВНО ОДНИМ словом, есть порча этой страницы."""
    слова = с.split(" ")
    for свои in _ПО_ДЛИНЕ.get(len(слова), ()):
        расход = 0
        for x, y in zip(свои, слова):
            if x != y:
                расход += 1
                if расход > 1:
                    break
        if расход == 1:
            return True
    return False


def _самопроверка():
    import re
    беды = []
    for с in ПОКАЗЫ:
        if судить(с) != (True, True):
            беды.append(f"честная не прошла: {с[:96]}")
    по_роду = {}
    for с, (я, р) in ПОКАЗЫ.items():
        по_роду[(я, р)] = по_роду.get((я, р), 0) + 1
    for язык in ЯЗЫКИ:
        for род in РОДЫ:
            if not по_роду.get((язык, род)):
                беды.append(f"род пуст: {язык}/{род}")
    # ТАБЛИЦЫ ОБЯЗАНЫ БЫТЬ ТЕМ, ЧЕМ НАЗВАНЫ.
    for т in ТРЕУГОЛЬНИКИ:
        if sum(т) != 180:
            беды.append(f"треугольник не даёт 180: {т}")
        if any(у <= 0 or у >= 180 for у in т):
            беды.append(f"угол вне границ: {т}")
        if sum(1 for у in т if у == 90) > 1:
            беды.append(f"два прямых угла в таблице: {т}")
    for ч in ЧЕТЫРЁХУГОЛЬНИКИ:
        if sum(ч) != 360:
            беды.append(f"четырёхугольник не даёт 360: {ч}")
    for a, b in ПРЯМОУГОЛЬНЫЕ:
        if a + b != 90:
            беды.append(f"острые не дают 90: {(a, b)}")
        if a <= 0 or b <= 0:
            беды.append(f"острый угол не положителен: {(a, b)}")
    for n in СТОРОНЫ:
        if n < 3:
            беды.append(f"сторон меньше трёх: {n}")
        if _сумма(n) != (n - 2) * 180:
            беды.append(f"общий закон не держится при {n}")
    # ОБЕ СТОРОНЫ ЛОВУШКИ ОБЯЗАНЫ БЫТЬ ПОКАЗАНЫ: постоянство И его граница.
    for язык in ЯЗЫКИ:
        if not (по_роду.get((язык, "иной треугольник"))
                and по_роду.get((язык, "четырёхугольник"))):
            беды.append(f"показана лишь одна сторона ловушки [{язык}]")
    # ПОСТОЯНСТВО ОБЯЗАНО БЫТЬ ВИДНО: треугольники обязаны РАЗЛИЧАТЬСЯ формой.
    if len({tuple(sorted(т)) for т in ТРЕУГОЛЬНИКИ}) < 4:
        беды.append("треугольники слишком похожи — постоянство суммы не видно")
    # ОБЩИЙ ЗАКОН ОБЯЗАН ПОКРЫВАТЬ ОБА ПОКАЗАННЫХ СЛУЧАЯ.
    if _сумма(3) != 180 or _сумма(4) != 360:
        беды.append("общий закон расходится с показанными случаями")
    # МУТАНТ ПО ЧИСЛУ В КАЖДОМ РОДЕ.
    for род in РОДЫ:
        страница = next((с for с, (я, р) in ПОКАЗЫ.items() if р == род and я == "ru"), None)
        if страница is None:
            continue
        m = re.search(r"= (\d+)", страница)
        if not m:
            continue
        порча = страница[:m.start(1)] + str(int(m.group(1)) + 1) + страница[m.end(1):]
        if судить(порча)[1] is not False:
            беды.append(f"подменённое число прошло в роде {род}: {порча[:70]}")
    # МУТАНТ ПО СЛОВУ.
    су = next(с for с, (я, р) in ПОКАЗЫ.items() if я == "ru" and р == "сумма треугольника")
    порча = су.replace("ТРЕУГОЛЬНИКА", "ТРЕУГОЛЬНИК", 1)
    if порча == су:
        беды.append("порча не состоялась: нет слова «ТРЕУГОЛЬНИКА»")
    elif судить(порча)[1] is not False:
        беды.append(f"обрезанное слово прошло: {порча[:70]}")
    if судить(су + " " + су)[0] is not False:
        беды.append("удвоение не осталось без вердикта")
    print(f"СУММА УГЛОВ: страниц {len(ПОКАЗЫ)}, родов {len(по_роду)}, бед {len(беды)}")
    for б in беды[:6]:
        print(f"  {б}")
    return 0 if not беды else 1


if __name__ == "__main__":
    sys.exit(_самопроверка())
