#!/usr/bin/env python3
"""ДОМ ОСНОВАНИЙ МАТЕМАТИКИ — и названный падеж.

Пять предметов математики и один языка стояли отсутствующими или тонкими. Здесь они стоят
вместе, ибо доказательство и падеж суть две стороны одного: сказать точно.

    ЧИСЛОВАЯ РАМКА ЕСТЬ СЕСТРА ПАДЕЖНОЙ, И РАМКА ОДНА С ПАРАМЕТРОМ, А НЕ ДВЕ. У английского
    нет падежа, но есть ЧИСЛО; разница языков — в ПОРЯДКЕ СЛОВ шаблона, и это свойство ЯЗЫКА,
    объявленное шаблоном, а не повод завести вторую рамку.

Семь родов объявлены не здесь: их назвала СВОЯ ЖЕ КУЗНИЦА кортежем `ГРУППЫ`, и род всякой
страницы известен по тому, ЧЬЯ ФУНКЦИЯ её сделала.

    РОД, РЕШЁННЫЙ ПОСТРОЕНИЕМ, УЖЕ ОБЪЯВЛЕН — ОСТАЁТСЯ ЛИШЬ ЗАПИСАТЬ ЕГО ТАМ, ГДЕ ЕГО ИЩУТ.

ПЕРЕЕЗЖАЕТ НЕ ФУНКЦИЯ, А ЗАМЫКАНИЕ ЦЕЛИКОМ: сюда переехали все семь строителей вместе с
`ЧИСЛОВОЙ_РАМКОЙ`, стоявшей ПОСРЕДИ них, — первая редакция этого переезда взяла тело с той
рамки и оставила пять строителей в кузнице, отчего дом пал на `NameError` при первом же ввозе.

    ГРАНИЦУ ЗАМЫКАНИЯ НЕЛЬЗЯ БРАТЬ ПО ПЕРВОЙ ЗАМЕТНОЙ СТРОКЕ: строители могут стоять и ДО
    неё. Брать надо от первого из них.
"""
import pathlib
import sys

КОРЕНЬ = pathlib.Path(__file__).resolve().parent.parent
sys.path.insert(0, str(КОРЕНЬ / "tools"))

import rugram  # noqa: E402
import units  # noqa: E402
from plural import by_count  # noqa: E402

# ПАДЕЖ — НЕ ОДИН РОД, А ШЕСТЬ (14.09). «Слово час в родительном падеже — часа» и «в
# творительном — часом» суть РАЗНЫЕ уроки: у каждого падежа своё окончание, своя работа в
# предложении и свой вопрос. Слитые в один род, они дали дому великана в 259 страниц при
# двенадцати у самого редкого — перекос 21,6×, и дом стоял среди крайних.
#
#     ШЕСТЬ ФОРМ, У КАЖДОЙ СВОЁ ОКОНЧАНИЕ, СУТЬ ШЕСТЬ РОДОВ, А НЕ ОДИН РОД «ПАДЕЖИ».
#     Имя «падежи» называет ПРЕДМЕТ урока, а род называет ДЕЛО, и дел тут шесть.
ПАДЕЖНЫЕ = tuple(f'падеж {п}' for п in
                 ('именительный', 'родительный', 'дательный',
                  'винительный', 'творительный', 'предложный'))

РОДЫ = ('функции', 'соответствия', 'мощность', 'от_противного', 'индукция',
        'числа') + ПАДЕЖНЫЕ

ЗАЧЕМ_РОДА = {
    "функции": "функция как правило, а не как таблица",
    "соответствия": "взаимно однозначное и его отсутствие",
    "мощность": "сколько элементов, когда их считать нельзя",
    "от_противного": "доказательство, идущее от отрицания",
    "индукция": "доказательство, идущее по ступеням",
    **{имя: f"{имя.split()[1]} падеж, НАЗВАННЫЙ, а не отсечённый — своё окончание у "
              f"двадцати девяти парадигм" for имя in ПАДЕЖНЫЕ},
    "числа": "числовая рамка — сестра падежной, и рамка одна с параметром",
}

def функции(шаг):
    """Соответствие как ОДИН предмет, применённое к точке."""
    вон = []
    for i in range(16):
        k = 2 + (i + шаг) % 4
        область = [1 + (i % 3), 2 + (i % 3), 3 + (i % 3)]
        точка = область[(i + шаг) % 3]
        если = " ".join(str(x) for x in область)
        вон.append(f"функция f на множестве {{{если}}} задана правилом "
                   f"f(x) = {k}x; f({точка}) = {k * точка}.")
        вон.append(f"the function f on the set {{{если}}} is given by "
                   f"f(x) = {k}x; f({точка}) = {k * точка}.")
    return вон


def соответствия(шаг):
    """Инъекция и её опровержение ОДНИМ свидетелем."""
    вон = []
    for i in range(10):
        k = 2 + (i + шаг) % 3
        область = [1 + (i % 4), 2 + (i % 4), 3 + (i % 4)]
        если = " ".join(str(x) for x in область)
        вон.append(f"функция f(x) = {k}x на множестве {{{если}}} "
                   f"инъективна: разные входы дают разные выходы.")
        вон.append(f"the function f(x) = {k}x on {{{если}}} is "
                   f"injective: different inputs give different outputs.")
        a, b = 1 + (i % 4), 3 + (i % 4)
        вон.append(f"функция f(x) = x mod 2 на множестве "
                   f"{{{a} {a + 1} {b} {b + 1}}} не инъективна: "
                   f"f({a}) = f({b}).")
        вон.append(f"the function f(x) = x mod 2 on "
                   f"{{{a} {a + 1} {b} {b + 1}}} is not injective: "
                   f"f({a}) = f({b}).")
    return вон


def мощность(шаг):
    """Мощность конечного и счётность бесконечного."""
    вон = []
    буквы = "a b c d e f g h".split()
    for n in range(1, 9):
        если = " ".join(буквы[:n])
        вон.append(f"мощность множества {{{если}}} равна {n}.")
        вон.append(f"the cardinality of the set {{{если}}} is {n}.")
    for i in range(6):
        n = 1 + i
        вон.append(f"чётные числа счётны: {n} отвечает {2 * n}, "
                   f"и это биекция с натуральными.")
        вон.append(f"the even numbers are countable: {n} maps to "
                   f"{2 * n}, and this is a bijection with the naturals.")
    return вон


def от_противного(шаг):
    """Допущение, ведущее к противоречию, — с вычислимым шагом."""
    вон = []
    for i in range(10):
        n = 3 + 2 * ((i + шаг) % 6)
        k, r = divmod(n, 2)
        # ИМЯ ХОДА СТОИ́Т НА СТРАНИЦЕ (12.09). Прибор [СВОЙСТВО НАЗВАНО] нашёл этот
        # ход показанным в одном мире и не названным нигде: страница вела рассуждение
        # и не давала ему имени, которым о нём говорят.
        вон.append(f"допустим, {n} чётно. тогда {n} = 2k для целого k. "
                   f"но {n} = 2 × {k} + {r}. противоречие: {n} нечётно. "
                   f"такой ход зовётся ДОКАЗАТЕЛЬСТВОМ ОТ ПРОТИВНОГО: "
                   f"допущенное привело к противоречию, и потому оно ложно.")
        вон.append(f"suppose {n} is even. then {n} = 2k for a whole k. "
                   f"but {n} = 2 × {k} + {r}. contradiction: "
                   f"{n} is odd. this step is called PROOF BY CONTRADICTION: "
                   f"what was assumed led to a contradiction, and so it is false.")
    return вон


def индукция(шаг):
    """Основание, шаг и покрытая ими цепь."""
    вон = []
    for i in range(6):
        верх = 3 + i
        сумма = верх * (верх + 1) // 2
        вон.append(f"индукция: основание n = 1 верно, шаг от n к n+1 "
                   f"верен, значит верно до n = {верх}; сумма первых "
                   f"{верх} чисел равна {сумма}.")
        вон.append(f"induction: the base n = 1 holds, the step from n "
                   f"to n+1 holds, hence it holds up to n = {верх}; the "
                   f"sum of the first {верх} {by_count(верх, 'numbers')} "
                   f"is {сумма}.")
    return вон


# ЧИСЛОВАЯ РАМКА ЕСТЬ СЕСТРА ПАДЕЖНОЙ, И РАМКА ОДНА С ПАРАМЕТРОМ, А
# НЕ ДВЕ. У английского нет падежа, но есть ЧИСЛО; у русского есть
# оба. Разница языков — в ПОРЯДКЕ СЛОВ шаблона (у английского имя
# формы стоит первым, у русского третьим), и это свойство ЯЗЫКА,
# объявленное шаблоном, а не повод завести вторую рамку.
ЧИСЛОВАЯ_РАМКА = {
    "en": ("the plural of {один} is {много}.",
           "the singular of {много} is {один}."),
    "ru": ("множественное число слова {один} — {много}.",
           "единственное число слова {много} — {один}."),
}


def числа(шаг):
    """Единственное и множественное, названные, а не только показанные.

    Форма выводится ПРАВИЛОМ письма и сверяется с объявленной парой:
    единица, чьё множественное правилу не подчиняется и в исключениях
    не названа, в показ НЕ ВЫХОДИТ — честное молчание вместо
    правдоподобной формы.
    """
    вон = []
    имена = sorted(units.ФОРМЫ_ВСЕХ)
    for i, имя in enumerate(имена):
        try:
            один, много = units.англ(имя), units.англ(имя, True)
        except (KeyError, IndexError):
            continue
        if units.мн_правилом(один) != много:
            continue
        мн_шаб, ед_шаб = ЧИСЛОВАЯ_РАМКА["en"]
        вон.append(мн_шаб.format(один=один, много=много))
        вон.append(ед_шаб.format(один=один, много=много))
        if (шаг + i) % 3 == 0:
            вон.append(f"what is the plural of {один}? the plural of "
                       f"{один} is {много}.")
            вон.append(f"what is the singular of {много}? the singular "
                       f"of {много} is {один}.")
    for один, много in sorted(units.МН_ИСКЛЮЧЕНИЯ.items()):
        вон.append(f"the plural of {один} is {много}, not "
                   f"{один + 's'}: it does not follow the rule.")
    return вон


def падежи(шаг):
    """Падеж НАЗВАН, а не только показан в работе; и род у каждого падежа СВОЙ."""
    from layer import Сбор                                # noqa: PLC0415
    вон = Сбор()
    for слово, формы in rugram.ПАРАДИГМЫ.items():
        for k, (падеж_в, форма) in enumerate(zip(rugram.ПАДЕЖИ_В, формы)):
            вон.род = ПАДЕЖНЫЕ[k]
            вон.append(f"слово {слово} в {падеж_в} падеже — {форма}.")
    # ИМЯ РЯДА СКАЗАНО ОДИН РАЗ ЗА ПРОХОД, А НЕ ПРИ КАЖДОМ СЛОВЕ:
    # парадигм стало двадцать девять, и повтор одной строки двадцать
    # девять раз есть мёртвая поверхность — сердце выучивает её
    # наизусть, и цена чтения исчезает.
    # СТРОКА О ШЕСТИ ПАДЕЖАХ ИДЁТ К ПЕРВОМУ ИЗ НИХ: она об ИХ ЧИСЛЕ, а не о седьмом деле,
    # и одной строкой на весь свод рода не составить.
    вон.род = ПАДЕЖНЫЕ[0]
    вон.append(f"падежей шесть: {', '.join(rugram.ПАДЕЖИ)}.")
    return вон

ГРУППЫ = (функции, соответствия, мощность, от_противного, индукция,
          падежи, числа)

# ИМЯ ГРУППЫ — НЕ ВСЕГДА ИМЯ РОДА: группа падежей называет свои строки САМА, по одному роду
# на падеж, и в этом ряду стои́т `None`. Шов мира при этом не движется: групп по-прежнему
# семь, и `emit_grouped` кладёт их тем же порядком.
ИМЕНА_ГРУПП = ('функции', 'соответствия', 'мощность', 'от_противного', 'индукция',
               None, 'числа')


def группы(шаг):
    """[[страница]] — ровно те группы и в том порядке, какими кузница кормит `emit_grouped`."""
    return [сделать(шаг) for сделать in ГРУППЫ]


def перебор(шаг):
    """[(страница, род)] — те же группы; группа, назвавшая строки сама, берётся построчно."""
    вон = []
    for род, группа in zip(ИМЕНА_ГРУПП, группы(шаг)):
        свои = getattr(группа, "роды", None)
        for i, с in enumerate(группа):
            вон.append((с, свои[i] if свои else род))
    return вон


def _показы():
    from layer import PASSES                              # noqa: PLC0415
    вон = {}
    for шаг in range(len(PASSES)):
        for с, род in перебор(шаг):
            вон.setdefault(с, род)
    return вон


ПОКАЗЫ = _показы()


def _самопроверка():
    """Объявление сверяется с ДЕЛОМ прохода, а не с кортежем.

    ГРУППА И РОД БОЛЬШЕ НЕ ОДНО И ТО ЖЕ (14.09): группа падежей называет свои строки сама,
    по одному роду на падеж, и потому число групп с числом родов не сходится. Сверяется то,
    что и должно: имена групп — с рядом `ИМЕНА_ГРУПП`, а всякий кованый род — с объявлением.
    """
    имена = tuple(с.__name__ for с in ГРУППЫ)
    ждём = tuple(и if и is not None else "падежи" for и in ИМЕНА_ГРУПП)
    assert имена == ждём, f"кортеж {имена} разошёлся с рядом групп {ждём}"
    сколько = len(группы(0))
    assert сколько == len(ГРУППЫ), f"проход даёт {сколько} групп при {len(ГРУППЫ)} строителях"
    кованые = {р for _с, р in перебор(0)}
    вне = кованые - set(РОДЫ)
    assert not вне, f"род кован и не объявлен: {sorted(вне)}"
    assert set(ЗАЧЕМ_РОДА) == set(РОДЫ), "глосса рода разошлась с объявлением"
    пустые = set(РОДЫ) - set(ПОКАЗЫ.values())
    assert not пустые, f"род объявлен и не кован: {sorted(пустые)}"


_самопроверка()
